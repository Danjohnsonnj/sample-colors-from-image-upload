{
  "name": "sample-image-colors",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "copy": "cpx 'src/**/*.{html,css}' './dist'",
    "copy:watch": "cpx 'src/**/*.{html,css}' './dist' --watch",
    "lint": "eslint src",
    "dev": "npm run lint && npm run copy && webpack --mode development",
    "build": "npm run lint && npm run copy && webpack --mode production",
    "watch": "npm run lint && (npm run copy:watch & webpack --mode development --watch)"
  },
  "repository": {},
  "dependencies": {
    "chroma-js": "^1.3.6"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-eslint": "^8.2.6",
    "babel-loader": "^7.1.5",
    "babel-preset-env": "^1.7.0",
    "cpx": "^1.5.0",
    "eslint": "^5.2.0",
    "webpack": "^4.16.1",
    "webpack-cli": "^3.1.0",
    "webpack-command": "^0.4.1"
  }
}
